# 贡献指南

感谢你帮助改进“一键式短片分镜”。提交修改前，请先确认建议能改善短片创作流程，而不会擅自替用户改变故事核心。

## 反馈问题

- 安装或调用故障：使用 Bug 模板，提供运行环境、复现步骤和完整错误信息。
- 输出质量问题：使用输出案例模板，提供输入、实际输出、期望行为，以及不可改动的故事核心。
- 新功能建议：说明真实使用场景和当前障碍，不必先设计实现方案。

请删除或替换文档链接、访问令牌、私人联系方式和未授权文本后再提交公开 Issue。

## 修改 Skill

1. 保持 `skills/idea-to-storyboard/SKILL.md` 简洁，把条件性方法和格式细节放入 `references/`。
2. 新增 reference 时，必须从 `SKILL.md` 或现有 reference 明确链接，并说明何时读取。
3. 不改变五项核心保护：内在问题、错误办法、关键选择、英雄镜头和结尾余味。
4. 不把某个案例中的角色、设定、世界规则或个人工具偏好写成所有用户的固定要求。
5. 修改安装路径、Skill 文件或分发包后，同步更新 README、教程和校验和。

## 本地检查

在仓库根目录运行：

```bash
python3 scripts/validate_repository.py
```

检查通过后再提交 Pull Request。请让每次修改聚焦一个问题，并在说明中写清验证结果。
